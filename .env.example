# Vogon Runtime provider credentials.
#
# Copy this file to .env for local shell tooling if needed. The vogon CLI reads
# environment variables from the process environment; it does not load .env
# files by itself.
#
# Keep values blank in this committed example. Do not commit real API keys.

GEMINI_API_KEY=
GROQ_API_KEY=
HF_TOKEN=
NVIDIA_API_KEY=
OPENROUTER_API_KEY=
OPENAI_COMPATIBLE_API_KEY=
